#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_ldapjs@1.0.2@ldapjs/bin/ldapjs-compare" "$@"
  ret=$?
else 
  node  "$basedir/../_ldapjs@1.0.2@ldapjs/bin/ldapjs-compare" "$@"
  ret=$?
fi
exit $ret
