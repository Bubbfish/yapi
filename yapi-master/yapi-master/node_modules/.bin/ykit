#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../_ykit@0.6.2@ykit/bin/ykit" "$@"
  ret=$?
else 
  node  "$basedir/../_ykit@0.6.2@ykit/bin/ykit" "$@"
  ret=$?
fi
exit $ret
