#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../../../_jerryproxy-ykit@1.1.2@jerryproxy-ykit/bin/jerry.js" "$@"
  ret=$?
else 
  node  "$basedir/../../../_jerryproxy-ykit@1.1.2@jerryproxy-ykit/bin/jerry.js" "$@"
  ret=$?
fi
exit $ret
